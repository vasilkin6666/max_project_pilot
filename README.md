# MAX Pilot v4.0 — PostgreSQL

## Запуск
```bash
docker-compose up -d
```

Готово:

PostgreSQL → localhost:5432
API → http://localhost:8000
Бот → @max_pilot_bot
Сайт → GitHub Pages

База
texthost: localhost
port: 5432
user: pilot
pass: supersecret
db: maxpilot
---
